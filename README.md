# Эмулятор командной оболочки ОС

**Практическая работа №1 | Вариант №4**

## 1. Общее описание
Разработать эмулятор для языка оболочки ОС. Необходимо сделать работу эмулятора  как  можно  более  похожей  на  работу  в  командной  строке  UNIX-подобной ОС.

**Текущий статус этапов разработки:**
- [x] Этап 1. REPL
- [x] Этап 2. Конфигурация
- [ ] Этап 3. VFS
- [ ] Этап 4. Основные команды
- [ ] Этап 5. Дополнительные команды

## 2. Описание всех функций и настроек
На текущем этапе реализован базовый цикл (REPL) и каркас для обработки конфигурации. Доступны следующие функции:
* **Парсинг ввода:** Разделение строки пользователя на основную команду и список аргументов.
* **Раскрытие переменных окружения:** Поддержка синтаксиса `$VAR_NAME`. Перед передачей в команду эмулятор подставляет реальные значения из словаря переменных (через `os.path.expandvars`).
* **Базовые команды:**
  * `ls` — команда просмотра директорий (выводит переданные аргументы).
  * `cd` — смена текущей директории (выводит переданные аргументы, логика VFS в разработке).
  * `exit` — выход из эмулятора.

## 3. Структура репозитория

```text
.
├── src/     
├── tests/        
├── scripts/       
├── .gitignore       
├── run.sh           
└── README.md        
```

**Запуск проекта:**
bash run.sh